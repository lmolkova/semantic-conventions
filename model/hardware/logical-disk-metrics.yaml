file_format: definition/2

attribute_groups:
  - id: metric_attributes.hw.logical_disk
    visibility: internal
    attributes:
      - ref_group: hardware.attributes.common
      - ref: hw.logical_disk.raid_level
        requirement_level: recommended

# LOGICAL DISK METRICS
metrics:
  - name: hw.logical_disk.limit
    stability: development
    brief: "Size of the logical disk."
    instrument: updowncounter
    unit: "By"
    annotations:
      code_generation:
        metric_value_type: int
    attributes:
      - ref_group: metric_attributes.hw.logical_disk

  - name: hw.logical_disk.usage
    stability: development
    brief: "Logical disk space usage."
    instrument: updowncounter
    unit: "By"
    annotations:
      code_generation:
        metric_value_type: int
    attributes:
      - ref_group: metric_attributes.hw.logical_disk
      - ref: hw.logical_disk.state
        examples: ["used", "free"]
        requirement_level: required

  - name: hw.logical_disk.utilization
    stability: development
    brief: "Logical disk space utilization as a fraction."
    instrument: gauge
    unit: "1"
    annotations:
      code_generation:
        metric_value_type: double
    attributes:
      - ref_group: metric_attributes.hw.logical_disk
      - ref: hw.logical_disk.state
        examples: ["used", "free"]
        requirement_level: required

metric_refinements:
  - id: hw.errors.logical_disk
    ref: hw.errors
    attributes:
      - ref_group: metric_attributes.hw.logical_disk

  - id: hw.status.logical_disk
    ref: hw.status
    attributes:
      - ref_group: metric_attributes.hw.logical_disk
