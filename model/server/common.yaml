file_format: definition/2
attribute_groups:
  - id: server
    visibility: public
    stability: stable
    brief: >
      General server attributes.
    attributes:
      - ref: server.address
      - ref: server.port
